import { PrescriptionDTO } from './prescription-dto.model';

describe('PrescriptionDTO.Model', () => {
  it('should create an instance', () => {
    expect(new PrescriptionDTO).toBeTruthy();
  });
});
